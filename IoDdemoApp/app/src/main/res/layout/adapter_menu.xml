<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:paddingBottom="5dp"
    android:paddingTop="5dp"
    android:background="#000000">

    <ImageView android:layout_width="64dp"
        android:layout_height="64dp"
        android:id="@+id/item_icon"
        android:src="@drawable/movie_icon"
        android:clickable="false"
        android:focusable="false"
        android:adjustViewBounds="true"
        android:layout_gravity="center_vertical"
        android:padding="3dp">
    </ImageView>

    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Movies" android:textSize="20sp" android:textColor="#ffffff" android:id="@+id/item_name"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="20dp">
    </TextView>

</LinearLayout>